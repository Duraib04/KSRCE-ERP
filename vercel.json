{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "flutter",
  "env": {
    "FLUTTER_CHANNEL": "stable"
  },
  "redirects": [
    {
      "source": "/:path((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ],
  "rewrites": [
    {
      "source": "/:path((?!.*\\.).*)",
      "destination": "/index.html"
    }
  ]
}
